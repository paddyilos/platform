# Liberia PBO Custom Backend Features

This fork of `ushahidi/platform` carries the backend for a small set of custom public features
built for the Liberia Peacebuilding Office (PBO): **Get Alerts**, **Contact Us**, **About Us**,
and **LERN Import**. See `ushahidi-client/LIBERIA_CUSTOM.md` (sibling repo) for the frontend
side of the same features, and `ireport/docs/MIGRATION_PLAN.md` (Phase 3) in the parent repo for
the full migration background.

## The pattern: minimize the stock-file diff

Almost every custom route/controller/model lives in its own new, isolated file with no
dependency on stock controllers — `AlertController`, `ContactUsController`,
`LernImportController`, `GeoLookupHelper`, and their models (`Alert`, `ContactUs`), all under
`src/Ushahidi/Modules/V5/`. New database tables get their own dedicated Phinx migrations,
prefixed `liberia_` for easy identification (`database/migrations/phinx/`):
`20260605000001_add_liberia_mgmt_levels.php`, `20260605000002_liberia_create_alerts_table.php`,
`20260630000001_liberia_add_location_to_alerts_table.php`,
`20260605000003_liberia_create_contact_us_table.php`.

Stock files are touched as little as possible:

| Stock file | What was added | Why |
|---|---|---|
| `src/Ushahidi/Modules/V5/ServiceProvider.php` (`boot()`) | 4-line `Route::prefix('api')->middleware('api')->namespace(...)->group(__DIR__.'/routes/liberia.php')` | Registers all Liberia routes in one hook. All actual route definitions live in `routes/liberia.php`, so this file's diff never grows as routes are added. |
| `src/Ushahidi/Modules/V5/Models/Config.php` | `about_us` added to `AVIALABLE_CONFIG_GROUPS` and `AVIALABLE_CONFIG_GROUPS_FOR_NON_ADMIN` | About Us reuses the stock generic config-group mechanism instead of a bespoke controller/migration — see `ushahidi-client/LIBERIA_CUSTOM.md` for the full rationale. |
| `src/Ushahidi/Modules/V5/Policies/ConfigPolicy.php` | `about_us` added to `$public_groups` (NOT `$readonly_groups`) | Makes the About Us config group publicly readable, admin-writable, same as `site`/`map`. |
| `src/Ushahidi/Modules/V5/Providers/EventServiceProvider.php` | `SendPostAlertsListener::class` added to both `PostCreatedEvent` and `PostUpdatedEvent` listener arrays | Hooks Get Alerts' notification-on-publish into the existing stock post-created/updated events — no `PostController.php` changes needed. See `SendPostAlertsListener.php` below. |
| `.env.example` | `APP_URL`, and a documented `MAIL_*` block (`MAIL_DRIVER`, `MAIL_HOST`, `MAIL_PORT`, `MAIL_USERNAME`, `MAIL_PASSWORD`, `MAIL_ENCRYPTION`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME`) | `config/mail.php` reads these but none were previously documented anywhere in the repo — mail was silently falling back to placeholder defaults (`smtp.mailgun.org`, no credentials). `APP_URL` is needed to build absolute unsubscribe/report links from the background alert listener, which has no HTTP request context to infer a host from. |
| `src/Ushahidi/Contracts/Permission.php` | `ACCESS_ANALYSIS = 'Access analysis'` constant | New permission gating the Analysis dashboard/templates feature. Exact string `Access analysis`, per PBO requirement — not the old UNICC-fork's longer `Access analysis and filters section`. |
| `src/Ushahidi/Modules/V5/Models/Post/Post.php` | `mgmt_lev_1`, `mgmt_lev_2` added to `ALLOWED_FIELDS` | These columns already existed (`20260605000001_add_liberia_mgmt_levels.php`, for Get Alerts/LERN) but were excluded from the default field list `ListPostsQuery`/`FindPostByIdQuery` build via `addOnlyParameteresFromRequest()`, so they never reached `PostResource`. Adding them here is the minimal change needed to expose county/district on `GET /posts` for the Analysis dashboard's reports-per-county chart, without touching `EloquentPostRepository::getGroupedTotals()`'s `group_by` switch statement (a bigger, riskier stock diff). Note: `$fillable` is a separate array governing mass-assignment, not field selection — it was NOT the right place for this (an earlier draft of this change assumed otherwise; corrected after tracing `ListPostsQuery::fromRequest()` directly). |
| `src/Ushahidi/Modules/V5/Models/Post/Post.php` | `incident_status` added to `ALLOWED_FIELDS` (not `$fillable`) | Exposes the new Incident Status column on `GET /posts`. Writes go through `PostController::patch()`'s `$post->setAttribute('incident_status', ...)` (see below), which bypasses mass-assignment/`$fillable` entirely, so — same lesson as the `mgmt_lev_1`/`mgmt_lev_2` row above — `$fillable` was correctly left untouched. |
| `src/Ushahidi/Contracts/Permission.php` | `SET_INCIDENT_STATUS = 'Set incident status'` constant | New permission gating writes to the Incident Status field, seeded to the `admin` role by `20260822000002_liberia_add_set_incident_status_permission.php`, then extended to `Management User`/`super`/`operatoruser` by `20260824000001_liberia_extend_incident_status_permission_roles.php` (below) to match the legacy allowlist, same shape as `ACCESS_ANALYSIS`. |
| `src/Ushahidi/Modules/V5/Requests/PostRequest.php` | `incident_status` rule (`nullable`, `Rule::in(IncidentStatus::all())`) added to `storeRules()`/`updateRules()` | Defense-in-depth validation for the full create/update (PUT) path, even though the primary write path is the `patch()` extension below. |
| `src/Ushahidi/Modules/V5/Http/Controllers/PostController.php` (`patch()`) | Extended the existing status-only PATCH endpoint (already flagged `@TODO: add all patch features. Right now we cover status only`) to also accept/persist an independent `incident_status` field, gated by a new private `requireCanSetIncidentStatus()` helper (same `service('authorizer.post')->acl->hasPermission()` template `AnalysisTemplateController`/`PivotDataController` use). The endpoint now requires at least one of `status`/`incident_status` in the body (previously `status` was unconditionally required). | Incident Status needs to be settable independently of the stock `status` (published/draft/archived) — see `ushahidi-client/LIBERIA_CUSTOM.md`'s "Incident Status" section for why the two must stay decoupled. `patch()` was already the intended extension point for exactly this per its own TODO comment. |
| `src/Ushahidi/Modules/V5/Http/Resources/PostResource.php` (the legacy resource `PostController::patch()` returns as `OldPostResource`, not the newer `Http/Resources/Post/PostResource.php`) | `incident_status` added to `includeResourceFields()`'s hardcoded field list, next to `status` | Found live: this resource's field list is hand-maintained (`mgmt_lev_1`/`mgmt_lev_2` aren't in it either) and doesn't dynamically follow `Post::ALLOWED_FIELDS`. Without this, a successful `PATCH .../posts/{id}` with `incident_status` silently omitted the new value from its own response — the DB write was correct, but the frontend's `IncidentStatusComponent`/`post-head` (which update `this.post` from the PATCH response, not a follow-up `GET`) would show the field reverting to unset until the next full reload. Confirmed via a live PATCH/GET round-trip before and after this fix. |
| `src/Ushahidi/Modules/V5/DTO/PostSearchFields.php` | New `protected $incident_status` field, populated in the constructor from `$request->get('incident_status')` (same `getParameterAsArray()` pattern as `tags`/`source`), and a new `incidentStatus(): array` accessor next to `status()` | Lets `GET /posts`/`/posts/stats` (and anything built on `PostSearchFields`, e.g. `PostStatsSearchFields extends PostSearchFields`) read the `incident_status[]` filter param the frontend now sends. |
| `src/Ushahidi/Modules/V5/Repository/Post/EloquentPostRepository.php` (`setSearchCondition()`) | `whereIn('posts.incident_status', ...)` added next to the existing `status` `whereIn`, but only applied when a new `userHasSetIncidentStatusPermission($user)` helper (mirrors `userHasManagePostPermissions()` immediately above it) returns true for the current `Auth::user()` | **Read-side permission enforcement, not just write-side.** Found live while verifying the frontend filter: unlike `status`, which `setGuestConditions()` (above) always forces to `published` for unprivileged/anonymous requests regardless of what the client asks for, an unguarded `incident_status[]` filter would have let *any* client — including anonymous — filter posts by this admin-only field, and a stale cached filter value from a previous admin browser session silently zeroed an anonymous visitor's results (reproduced live: a leftover `incident_status=evaluated` in `localStorage` from an earlier admin session caused a logged-out reload of `/feed` to return 0 posts, since the one post carrying that value wasn't `published`). Gating the filter application itself — the same enforcement model already used for `status` — fixes both the real permission gap and that reproduced bug at once, rather than trying to chase down every frontend code path that might carry a stale value. |
| `src/Ushahidi/Modules/V5/routes/liberia.php` | 5 new `v5/analysis-templates*` route lines, plus 1 `v5/analysis-pivot-data` route line | Registers the Analysis Templates CRUD API and the Report Builder's pivot-data feed. Uses `v5/` (unlike the existing `v3/get-alerts` etc. entries above) to match the stock API's actual version convention — the `v3/` prefix on the older routes is a known pre-existing inconsistency, not one to replicate going forward. |
| `src/Ushahidi/Modules/V5/Repository/Post/EloquentPostRepository.php` | Implemented `case 'attribute':` in `getGroupedTotals()` (previously an empty `break;`, dead code, not a Liberia regression — see `tests/Integration/stats.feature`'s pre-existing scenario, which asserted only a 200 status and never actually exercised grouping since it used the wrong query param name, `attribute_key` instead of `group_by_attribute_key`, fixed alongside this change) | The Analysis Report Builder's per-attribute grouping (parity with the old platform's Flexmonster pivot-by-attribute) needs this. New private `applyGroupByAttribute()` looks up the `Attribute` by `key`, joins the matching EAV value table (`post_varchar`/`post_text`/`post_datetime`/`post_decimal`/`post_int`, resolved from `form_attributes.type`) on `form_attribute_id`, and groups by `value` — mirrors the existing `tags` case's join/groupBy shape. Rejects (`ValidatorException`, → HTTP 422) a missing key or an ungroupable type (`point`/`geometry`/`media`) instead of silently misbehaving. |
| `src/Ushahidi/Modules/V5/Actions/Auth/Handlers/PasswordResetCommandHandler.php` (V5 password reset) | `$mailer->send(...)` wrapped in try/catch + `\Log::warning('Password reset email failed: ...')` | Previously uncaught — a delivery failure either 500'd the request or (depending on `MAIL_DRIVER`) failed with no signal at all. Matches the same log-and-continue convention already used by `ContactUsController`/`SendPostAlertsListener` below. |
| `src/Ushahidi/Core/Usecase/User/GetResetToken.php` (V3 password reset, stock core usecase) | Same treatment: `$this->mailer->send(...)` wrapped in try/catch + `\Log::warning('Password reset email failed: ...')` | Same reasoning as the V5 handler above — this is the parallel legacy `v3/passwordreset` code path, also previously unguarded. |
| `src/Ushahidi/Core/Tool/Mailer.php` (`sendResetpassword()`) | Added `site_email` to the `$data` array passed to the view; changed the `from()` call from conditional (`if ($site_email) { $message->from($site_email, $site_name); }`) to unconditional, always using `$site_name` as the sender display name and falling back to `config('mail.from.address')` only for the address half. Also replaced `$this->getSite()->getName()`/`getEmail()`/`getClientUri()` (the `UsesSiteInfo` trait) entirely with a direct query against `Ushahidi\Modules\V5\Models\Config` for the `site` group's `name`/`email`/`client_url` keys, and fixed the `client_url` fallback from `??` to `?:` | De-branding the password-reset email (see `forgot-password.php` below) needed the site's contact email available in the view, and the sender display name needed to reliably be the deployment name rather than silently falling back to the `MAIL_FROM_NAME` env var (which can drift out of sync with Settings → General's "Name" field) whenever `site_email` happens to be unset. Separately, discovered live that `Site::getSiteConfig()` (which `getSite()->getName()`/`getEmail()`/`getClientUri()` depend on) unreliably returns stale/empty data for the `site` config group in this deployment — confirmed via a side-by-side tinker test: a raw query against the exact same DB connection object returns the real `name`/`email`/`client_url` values, but the legacy AuraDI-constructed `ConfigRepository` behind `Site::getSiteConfig()` does not, for reasons not fully root-caused (see the platform-wide risk note below). The V5 Eloquent `Config` model reliably returns the correct values, so `sendResetpassword()` now reads from it directly instead of routing through the unreliable `Site` accessors — scoped to this one email rather than touching the shared `Site`/`ConfigRepository` classes used platform-wide. `getClientUri()`'s default return value is `false` (not `null`), so the original `?? env('DEFAULT_CLIENT_URL')` never actually triggered its fallback (`??` only catches `null`) — `?:` does. |
| `src/Ushahidi/Modules/V5/Models/Category.php` (`children()`) | Added `'parent'` to the relation's existing `->with('translations')` call — now `->with(['translations', 'parent'])` | Stock bug, found live while verifying the categories dedup fix above: `CategoryResource::toArray()` only includes a `parent` key when that relation is already loaded on the model instance, but `children()` never eager-loaded it — so any category reached via a parent's `children` array (as opposed to its own top-level list entry) silently lost its `parent` object in `GET /categories` responses. This alone was often masked by `RequestCachedResource`'s per-request cache (keyed only by category ID, not by which relations happen to be loaded) reusing an earlier, correctly-shaped resolution of the same ID — but a category whose ID is lower than its parent's gets resolved via the broken `children` path *first* in ID-descending list order, and that broken shape then spreads to every other appearance of that ID in the same response, including its own top-level entry. Confirmed on two categories with real parent assignments (one from this session, one pre-existing) before this fix; the client's `categories.component.html` reads `category.parent.slug` for a nested child's test-hook attribute, so the missing `parent` threw and blanked that category's row in the Settings → Categories tree (see `ushahidi-client/LIBERIA_CUSTOM.md`'s matching entry). One-line fix, same eager-loading pattern already on that line. |
| `resources/views/emails/forgot-password.php` | Removed the Ushahidi wordmark logo (both the header and footer instances) and the footer's Ushahidi mission-statement tagline; replaced `support@ushahidi.com` with `<?php echo $site_email; ?>` and "The Ushahidi Team" with `The <?php echo $site_name; ?> Team` | Stock Ushahidi branding in the password-reset email — logo, support address, sign-off — had never been touched by this fork (confirmed via `git log --follow`: only upstream Ushahidi contributors, through 2023-07-26) and doesn't reflect this deployment. `$site_name` was already passed to this view; `$site_email` is now passed too (see `Mailer.php` above). |

## New isolated files (no stock-file diff)

| File | Purpose |
|---|---|
| `src/Ushahidi/Modules/V5/routes/liberia.php` | All Liberia route definitions (`get-alerts`, `contact-us`, `lern-import`). |
| `src/Ushahidi/Modules/V5/Http/Controllers/AlertController.php` | Get Alerts subscribe/unsubscribe/location-lookup. |
| `src/Ushahidi/Modules/V5/Models/Alert.php` | `alerts` table model. |
| `src/Ushahidi/Modules/V5/Listeners/SendPostAlertsListener.php` | Emails Get Alerts subscribers when a published report matches their radius/category. Ports `SendAlertsJob` from the old UNICC backend (`ireport/backend/src/App/Listener/SendAlertsJob.php`), which was never carried over when this fork was built — until this listener was added, subscribing to Get Alerts stored a row and did nothing else. Registered via `EventServiceProvider.php` above. |
| `resources/views/emails/post-alert.php` | Plain-PHP email view for the above (same convention as `resources/views/emails/forgot-password.php` — not Blade). |
| `src/Ushahidi/Modules/V5/Helpers/GeoLookupHelper.php` | Resolves a lat/lng to a Liberia county/district name for the Get Alerts location field. |
| `src/Ushahidi/Modules/V5/Http/Controllers/ContactUsController.php` | Public contact form; sends a notification email directly via `Mail::send()` (see `Mailer.php` below for why this bypasses the `Mailer` tool). |
| `src/Ushahidi/Modules/V5/Models/ContactUs.php` | `contact_us` table model. |
| `src/Ushahidi/Modules/V5/Http/Controllers/LernImportController.php` | LERN data import (admin only) — backend scaffolding, see `ireport/docs/MIGRATION_PLAN.md` Phase 3b. |
| `database/migrations/phinx/20260605000001_add_liberia_mgmt_levels.php`, `20260605000002_liberia_create_alerts_table.php`, `20260630000001_liberia_add_location_to_alerts_table.php`, `20260605000003_liberia_create_contact_us_table.php` | New tables/columns for the features above. |
| `src/Ushahidi/Modules/V5/Http/Controllers/AnalysisTemplateController.php` | CRUD for saved Analysis dashboard report templates. Enforces `Permission::ACCESS_ANALYSIS` itself via `service('authorizer.post')->acl->hasPermission()` (defense in depth — same pattern `PostController` uses for permission checks; reuses the existing `authorizer.post` binding rather than registering a new `authorizer.*` binding since `Acl::hasPermission()` is generic). `report_config` validates only as `nullable|array` — since the Report Builder moved to WebDataRocks (see `ushahidi-client/LIBERIA_CUSTOM.md`), each entry is an opaque WebDataRocks `Report` object (`slice`/`options`/`conditions`/`formats`, library-defined), not a fixed `{group_by, chart_type}` shape the backend can usefully validate sub-fields of. |
| `src/Ushahidi/Modules/V5/Models/AnalysisTemplate.php` | `analysis_templates` table model. `$fillable`/`$casts` extended for `report_config`/`status_filter`/`tags_filter` (see below); the original `group_by`/`group_by_attribute_key`/`chart_type` columns are kept as an unused unpopulated legacy artifact from before the WebDataRocks rebuild, for backward compatibility with any row created while that schema was live. |
| `src/Ushahidi/Modules/V5/Http/Controllers/PivotDataController.php` | Lean, flat per-post rows (`{Post ID, Status, Date, Title, County, District, <attribute label>: value, ...}`) feeding the Report Builder's WebDataRocks pivot table. **Not** the stock `GET /posts?only=...,post_content` — that returns full field *metadata* (label/input/type/options/config/translations) repeated on every post, measured at ~5.8KB/post against this deployment's real data (blows past WebDataRocks' 1MB free-tier cap at ~170 posts). This endpoint is ~20x leaner (~255 bytes/post measured), batches one query per EAV value table (`post_varchar`/`post_text`/`post_datetime`/`post_decimal`/`post_int`) instead of per-post Eloquent relationship loading, and hard-caps at `limit` (default/max 3000, `truncated: true` in the response if the real total exceeds it) — verified live: the entire 1,677-post "Incident Reports (2023-)" survey's dataset comes back at ~428KB, well under the cap. Enforces `Permission::ACCESS_ANALYSIS` the same way `AnalysisTemplateController` does. Applies the same attribute exclusion rule as `EloquentPostRepository::applyGroupByAttribute()` (Phase 0 above) and the frontend's dynamic field discovery: only `varchar`/`text`/`datetime`/`decimal`/`int` attribute types, excluding plain free-text (`input=text` + `type=varchar`) fields. |
| `database/migrations/phinx/20260701000001_liberia_add_access_analysis_permission.php` | Seeds the `Access analysis` permission and grants it to the `admin` role. |
| `src/Ushahidi/Addons/LiberiaSms/composer.json`, `ServiceProvider.php`, `HttpSmsStructuredPostListener.php` | Parses the legacy semicolon-delimited SMS report format (`Title; Description; Type code; Indicator code; Date; Location`) into structured survey fields for messages received via the `httpsms` data source, instead of the stock `CreatePostFromMessage` behaviour of dumping the entire raw SMS into the post's content. Fully dynamic — discovers the target survey's own fields at runtime (`FormAttributeRepository::getByForm()`), matching SMS type/indicator codes against whichever `varchar` attributes are labeled `"... Indicators"` and their own `options` arrays, rather than hardcoding a form id or attribute keys; any SMS part with nowhere to go on the current survey folds into the Description text instead of being dropped. **Unlike every other row in this table, this is a genuine zero-stock-diff addition**: packaged as its own composer package (same shape as `Addons/HttpSMS`), auto-discovered via the existing `src/Ushahidi/**/*/composer.json` merge-plugin glob in the root `composer.json` (already present for `Addons/HttpSMS`/`Addons/Infobip`/etc. — not added for this), so it needs no `EventServiceProvider.php` edit at all: `ServiceProvider::boot()` calls `Event::listen('message.receive', ...)` directly, and Laravel boots package-discovered providers before `config/app.php`-listed ones (confirmed via `LaravelPackageManifest`/Laravel's own provider-registration order), guaranteeing this fires before `Modules\V3\Listener\CreatePostFromMessage`. Chosen specifically so a future `ushahidi-api` submodule bump can never silently overwrite or lose this — there is no upstream file at this path to conflict with. |
| `database/migrations/phinx/20260701000002_liberia_create_analysis_templates_table.php` | Creates the `analysis_templates` table. |
| `database/migrations/phinx/20260702000001_liberia_analysis_templates_multi_chart.php` | Extends `analysis_templates` for multi-pivot reports and template-wide filters: renames the previously-dead `filters` column (declared but never populated by any frontend code) to `report_config`, repurposed to hold a JSON array of WebDataRocks report objects (one per `<app-wbr-pivot>` panel); adds `status_filter`/`tags_filter` JSON columns. Backfills any pre-existing single-chart row into a one-element `report_config` array so it still renders as a 1-pivot report. See `ushahidi-client/LIBERIA_CUSTOM.md`'s "Analysis / Analysis Templates" section for the frontend shape. |
| `migration/migrate-liberia-analysis-templates.php` | One-off, manually-run script (same PDO/dual-connection/`log_step()` conventions as `migrate-liberia.php`, but a separate script since the main migration already ran) that best-effort ports the real saved templates from the old `config` table (group `filters`, Flexmonster pivot-config JSON) into the new `analysis_templates` schema. Preserves each old 2D pivot (`rows` × `columns`) largely verbatim rather than decomposing it, remapping only each dimension's `uniqueName` — from the old Flexmonster field name (e.g. "Province", "Type of Incident") to the exact column name `PivotDataController` produces (e.g. "County", or a migrated `form_attributes.label`) — dropping (and logging) individual dimensions that don't map (e.g. `tags`-typed attributes, which `PivotDataController` doesn't expose at all) rather than the whole chart; old per-member `filter` values are stripped, not carried over, since they reference old category/tag IDs meaningless on the new dataset. Must run after the `20260702000001` migration above. **Not idempotent** — a plain `INSERT`, not `INSERT IGNORE`; running it twice creates duplicate templates (same one-off-script convention as `migrate-liberia.php`, meant to run once during the real cutover). **Verified live** against the real production data dump: all 7 saved templates processed, 6 migrated (1 had no `reportConfig` at all in the old data, correctly skipped), each migrated template's "Province" dimension correctly remapped to "County" and confirmed rendering a real county-level crosstab with accurate counts after applying it in the actual UI. |
| `src/Ushahidi/Modules/V5/Models/Post/IncidentStatus.php` | Allowed-values constant list for Incident Status (`verification_in_progress`/`unverified`/`verified`/`responded`/`evaluated`), same shape as the stock `PostStatus.php` it sits next to. |
| `database/migrations/phinx/20260822000001_liberia_add_incident_status_to_posts.php` | Adds the nullable `posts.incident_status` VARCHAR(30) column. `posts.status` is a plain VARCHAR (not a SQL enum, confirmed in the original `20140716082651_initial.php` migration), so this is a simple additive column — no risk from widening/altering an existing type. |
| `database/migrations/phinx/20260822000002_liberia_add_set_incident_status_permission.php` | Seeds the `Set incident status` permission and grants it to the `admin` role, same template as `20260701000001_liberia_add_access_analysis_permission.php`. |
| `database/migrations/phinx/20260824000001_liberia_extend_incident_status_permission_roles.php` | Grants the already-seeded `Set incident status` permission to `Management User`/`super`/`operatoruser` as well, matching the legacy fork's role allowlist — see the "Incident Status" section above. |
| `migration/migrate-liberia-dedupe-categories.php` | One-off, manually-run script (single-connection PDO against the already-migrated `ushahidi` DB only — no source-DB dependency, unlike `migrate-liberia.php`) that disambiguates the ~13 groups of duplicate `tags.tag` names left over from `migrate-liberia.php`'s verbatim copy (e.g. "Physical violence" ×5, confirmed against the real production dump, including two genuine same-`type` collisions — two "Test"/`type=category` rows — that get an extra `#id` suffix). Ushahidi's stock `PUT /categories/{id}` validation (`src/Ushahidi/Modules/V5/Requests/CategoryRequest.php`, `unique:tags,tag,<id>`) rejects any edit — including just assigning a parent — to a category whose `tag` collides with another row's, since the frontend always resubmits the unchanged `tag` too; this silently blocked re-parenting the migrated categories entirely (see `ushahidi-client/LIBERIA_CUSTOM.md`'s `create-category-form` entry for the companion fix that makes this class of failure visible in the UI going forward). Renames each duplicate row to `"{tag} ({TypeLabel})"`, disambiguating using the legacy `type` column (the old platform's ad-hoc grouping key) via a small lookup table (`category`→`Category`, `health-and-environment`→`Health & Environment`, etc.), falling back to a title-cased slug for any unmapped `type` and to an `#id` suffix for same-type collisions within one group — both cases explicitly logged via `log_step()`. Naturally safe to rerun (re-detects duplicates from current state each time; after a successful run there are none left), unlike the plain-`INSERT` one-off scripts elsewhere in this directory. |

## docker-compose.local.yml: mysql-data volume

`docker-compose.local.yml` (added by an earlier commit, not itself new here) is
the designated file for Liberia-specific local-dev infra overrides — it
already overrides `mysql`'s `container_name`/`ports`/`environment`. The stock
`docker-compose.yml`'s `mysql` service has **no `volumes:` entry at all**,
so MariaDB's data directory lives only in the container's writable layer —
a plain `docker compose down` (which removes containers, not just stops
them) destroys the database. This caused a real incident: the ~5,687-post
LERN dataset (migrated in via `migration/migrate-liberia.php`) was wiped by
a routine cleanup `down` and had to be re-migrated from the old platform's
still-running database.

Fix: `docker-compose.local.yml` now mounts a named volume,
`mysql-data:/var/lib/mysql`, on the `mysql` service, with a matching
top-level `volumes: { mysql-data: }` declaration. Docker Compose merges
service-level `volumes:` lists and top-level named-volume declarations
across `-f` files (not last-file-wins), so this applies cleanly on top of
the stock `mysql` service without touching `docker-compose.yml` — verified
by inspecting `docker compose -f docker-compose.yml -f
docker-compose.local.yml config` and by reproducing the exact `down`/`up`
cycle that caused the original loss, confirming data now survives it.
`docker compose down -v` still removes named volumes by design; only a
plain `down` is protected against.

## Incident Status

Restores the legacy UNICC fork's admin-only "Incident Status" workflow field
(`ireport/backend/src/App/Validator/Post/Create.php`'s extended `status` values —
`responded`/`evaluated`/`verification` — and the frontend's 6-button status group), which
had no equivalent on this fork. The legacy app kept this workflow state **independent**
of whether a post was actually published (a separate `post.published` boolean, decoupled
from `post.status`) — an incident could be `Evaluated` and still be published or
unpublished. Folding the new values into the stock `status` column (published/draft/
archived) would have broken that independence, since `status` is a single
mutually-exclusive value that also drives this fork's public-visibility logic. Instead,
Incident Status is a **new, separate column** (`posts.incident_status`), fully decoupled
from `status` — see `ushahidi-client/LIBERIA_CUSTOM.md`'s matching "Incident Status"
section for the frontend UI, which surfaces it as its own admin-only control, not folded
into the existing Publish/Archive UI.

Gated end-to-end by `Permission::SET_INCIDENT_STATUS`, checked server-side the same way
`Permission::ACCESS_ANALYSIS` is (`service('authorizer.post')->acl->hasPermission()`) —
the client-side permission check is convenience only, not the security boundary.

**Not actually admin-only in legacy — matched here, not simplified.** The legacy fork's
equivalent check (`ireport/backend/src/Core/Traits/AdminAccess.php`'s
`isUserAllowedToUpdateStatus()`) gated the extended status values
(`verification`/`responded`/`evaluated`) to a 4-role allowlist: `admin`, `super`,
`Management User`/`managementuser`, `operatoruser` — not `admin` alone. Roles with
broader "Access incident/risk reports" access but off that allowlist (`sysadmin`,
`Regional User 1`, `Regional User 2`, `viewer1`, `user`) could not set these values.
`20260822000002_liberia_add_set_incident_status_permission.php` only seeded `admin`,
which under-granted relative to legacy for the 4 real `Management User` accounts in this
deployment's migrated data (see `migration/migrate-liberia.php`'s verbatim `roles`/`users`
copy). `20260824000001_liberia_extend_incident_status_permission_roles.php` grants the
same permission to `Management User` (the exact `roles.name` value carried over by that
copy) plus `super`/`operatoruser` for parity with the legacy allowlist. Those two roles
don't exist in this deployment's migrated data (deleted from Liberia's production DB
before the migration dump was taken) — found live, the hard way: `roles_permissions.role`
has a foreign key to `roles.name` (`roles_permissions_ibfk_1`), so the migration also
creates the two role rows themselves (`protected = 0`, no users) before granting them the
permission, otherwise the `INSERT` 1452s. No code changes were needed for the permission
check itself — `PostController::requireCanSetIncidentStatus()` and
`EloquentPostRepository::userHasSetIncidentStatusPermission()` both already do a generic
`roles_permissions` lookup keyed by the user's role, same as every other permission.

## Why `ContactUsController`/`SendPostAlertsListener` use `Mail::send()` directly

`src/Ushahidi/Core/Tool/Mailer.php` (a stock file) only implements one mail type,
`resetpassword`, dispatched via reflection (`'send'.Str::ucfirst($type)`). Rather than extending
that stock class with Liberia-specific mail types — which would mean editing a Core file every
time a new custom email is added — both `ContactUsController` and `SendPostAlertsListener` call
`Illuminate\Support\Facades\Mail::send()` directly, wrapped in try/catch + `\Log::warning()` so a
delivery failure never breaks the request/listener. `SendPostAlertsListener` does still reuse
the stock `env('DEFAULT_CLIENT_URL')` pattern from `Mailer.php` for building the frontend report
link, for consistency.

## Staying in sync with upstream

Merge from upstream's latest **release** branch, not `develop` — `develop` moves continuously
and pulls in unreviewed/unreleased work, whereas a `release/*` branch is a stable, tagged
snapshot. Check `https://github.com/ushahidi/platform/branches` (or `git branch -a | grep
release/`) for the newest one before merging; as of this writing that's `release/2025.04`.

```bash
git fetch upstream
git merge upstream/release/2025.04   # replace with whatever is newest
```

Expect conflicts only in the small number of stock files listed in the first table above —
everything else is additive, isolated files.
